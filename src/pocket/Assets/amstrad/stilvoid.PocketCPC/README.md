Place the required `boot.rom` bundle here.

Optional experimental custom upper ROM:

- `custom.rom`
- exactly 16 KiB
- mapped to CPC upper ROM slot `8`

Optional `.dsk`, `.cdt`, and `.sna` files belong under `/Assets/amstrad/common/`.
